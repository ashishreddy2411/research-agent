# Research Agent — Environment Variables
# Copy this file to .env and fill in your values.
# .env is git-ignored. Never commit real credentials.

# ── Azure AI Foundry ───────────────────────────────────────────────────────────
FOUNDRY_ENDPOINT=https://<resource>.services.ai.azure.com/api/projects/<project>
FOUNDRY_API_KEY=<your-key>

# ── Models (must match deployment names in your Foundry project) ──────────────
SMART_MODEL=gpt-5.2-chat
CHEAP_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small

# ── Search ────────────────────────────────────────────────────────────────────
# Get your free key at: https://app.tavily.com
TAVILY_API_KEY=tvly-<your-key>

# ── Research loop tuning (optional — defaults are sensible) ───────────────────
MAX_RESEARCH_ROUNDS=3
MAX_SOURCES_PER_RUN=50

# ── Cost control ──────────────────────────────────────────────────────────────
# Hard stop: if a run exceeds this cost, stop and return partial results.
MAX_COST_USD=2.0
